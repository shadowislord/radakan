For now, I assume that the program is run in a directory with the configuration files (or a link to them). The last command line argument should be the path of the trunk direcory.

At shutdown the program gives an error.
